module.exports = {
    PROXY: 'http://localhost:8000',                         // 这块的端口号其实是对应 node 服务里启动的端口号
    PORT: 8088,                                             // 这个端口号是 browser-sync 插件代理的端口号
};